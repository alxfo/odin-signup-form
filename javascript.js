const pwdvalidation = document.querySelector('.pwd-validation');
const passwordInput1 = document.querySelector('.password');
const passwordInput2 = document.querySelector('.conf-password');

// if (passwordInput1.textContent !== passwordInput2.textContent) {
//     pwdvalidation.textContent = 'Passwords must match'
// } else {
//     pwdvalidation.textContent = 'Passwords match'
// }